image: python:3.11

stages:
  - lint
  - test

before_script:
  - python -m venv .venv
  - source .venv/bin/activate
  - pip install -e .[test,lint]

lint:
  stage: lint
  script:
    # - ruff check .
    - pylint -E --ignore tui.py src examples

test:
  stage: test
  script:
    - pytest
